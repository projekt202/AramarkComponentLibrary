import { ButtonConfig } from '../button/button.component.model';
export declare type FullwidthImageCardCta = {
    variant: 'link' | 'arrow-link';
    config: ButtonConfig & {
        underline?: boolean;
    };
};
