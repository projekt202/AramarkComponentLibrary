export interface NumberedPagination {
    index: number;
    maxPages: number;
    pages: number[];
}
export declare enum RulerFactoryOption {
    Start = "START",
    End = "END",
    Default = "DEFAULT"
}
